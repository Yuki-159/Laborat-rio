#include <stdio.h>

int main()
{
    int numeros[10];
    for (int i = 0; i, 10; i++)
    {
        printf("Diga um valor de I: ");
        scanf("%i", &i);
    }
    int numero;
    printf("Diga um numero: ");
    scanf("%i", &numero);
    int achou = -1;
    for (numeros[i] == numero)
    {
        achou = i;
    }

    return 0;
}